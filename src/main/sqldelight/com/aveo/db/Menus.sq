CREATE TABLE IF NOT EXISTS menuItem (
    id INTEGER AS Int NOT NULL UNIQUE PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS menu (
    id INTEGER AS Int NOT NULL UNIQUE PRIMARY KEY AUTOINCREMENT,
    date INTEGER NOT NULL,
    meal TEXT NOT NULL
);

INSERT OR REPLACE INTO menuItem (name) VALUES (?);

